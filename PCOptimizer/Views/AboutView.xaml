<UserControl x:Class="PCOptimizer.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource DeepBlackBrush}"
             Foreground="{DynamicResource PrimaryTextBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{DynamicResource DeepBlackBrush}">
        <Grid Margin="24" MaxWidth="800">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- App Info Card -->
            <Border Grid.Row="0" Style="{DynamicResource CardStyle}" Margin="0,0,0,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Application Information" Style="{DynamicResource H3TextBlockStyle}"
                              Margin="0,0,0,16" />

                    <StackPanel Grid.Row="1" Margin="0,0,0,12">
                        <TextBlock Text="App Name" Style="{DynamicResource BodyTextBlockStyle}"
                                  Margin="0,0,0,4" />
                        <TextBlock Text="PC Optimizer" Style="{DynamicResource SecondaryTextBlockStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,0,0,12">
                        <TextBlock Text="Version" Style="{DynamicResource BodyTextBlockStyle}"
                                  Margin="0,0,0,4" />
                        <TextBlock Text="1.0.0 (Build 2025.01.15)" Style="{DynamicResource SecondaryTextBlockStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Row="3" Margin="0,0,0,12">
                        <TextBlock Text="Company" Style="{DynamicResource BodyTextBlockStyle}"
                                  Margin="0,0,0,4" />
                        <TextBlock Text="Event Horizon / Universal EDA" Style="{DynamicResource SecondaryTextBlockStyle}" />
                    </StackPanel>

                    <StackPanel Grid.Row="4">
                        <TextBlock Text="Tagline" Style="{DynamicResource BodyTextBlockStyle}"
                                  Margin="0,0,0,4" />
                        <TextBlock Text="Automation. Optimization. Configuration." Style="{DynamicResource SecondaryTextBlockStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- System Specifications Card -->
            <Border Grid.Row="1" Style="{DynamicResource CardStyle}" Margin="0,0,0,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="System Information" Style="{DynamicResource H3TextBlockStyle}"
                              Margin="0,0,0,16" />

                    <Grid Grid.Row="1" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Processor" Style="{DynamicResource BodyTextBlockStyle}"
                                      Margin="0,0,0,4" />
                            <TextBlock Text="Intel Core i7-9700K @ 3.60GHz"
                                      Style="{DynamicResource SecondaryTextBlockStyle}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Memory (RAM)" Style="{DynamicResource BodyTextBlockStyle}"
                                      Margin="0,0,0,4" />
                            <TextBlock Text="16.0 GB DDR4-3200" Style="{DynamicResource SecondaryTextBlockStyle}" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="2" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Graphics" Style="{DynamicResource BodyTextBlockStyle}"
                                      Margin="0,0,0,4" />
                            <TextBlock Text="NVIDIA GeForce RTX 3070 (8GB)"
                                      Style="{DynamicResource SecondaryTextBlockStyle}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Operating System" Style="{DynamicResource BodyTextBlockStyle}"
                                      Margin="0,0,0,4" />
                            <TextBlock Text="Windows 11 Pro (Build 22621)"
                                      Style="{DynamicResource SecondaryTextBlockStyle}" />
                        </StackPanel>
                    </Grid>

                    <Separator Grid.Row="3" Background="{DynamicResource GoldMediumBrush}" Margin="0,12,0,12" Height="1" />

                    <StackPanel Grid.Row="4">
                        <TextBlock Text="✓ ML Model: Ready (82 data points)"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Foreground="{DynamicResource SuccessBrush}"
                                  Margin="0,4,0,4" />
                        <TextBlock Text="✓ Anomaly Detection: Enabled"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Foreground="{DynamicResource SuccessBrush}"
                                  Margin="0,4,0,4" />
                        <TextBlock Text="✓ History Buffer: 82/100 metrics stored"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Foreground="{DynamicResource SuccessBrush}"
                                  Margin="0,4,0,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Features Card -->
            <Border Grid.Row="2" Style="{DynamicResource CardStyle}" Margin="0,0,0,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Key Features" Style="{DynamicResource H3TextBlockStyle}"
                              Margin="0,0,0,16" />

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="✅ Real-time performance monitoring (CPU/GPU/RAM/Temp)"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4" />
                        <TextBlock Text="✅ ML-powered anomaly detection (UNIQUE SELLING POINT)"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4"
                                  Foreground="{DynamicResource GoldBrightBrush}" />
                        <TextBlock Text="✅ Professional industrial aesthetic with Event Horizon branding"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4" />
                        <TextBlock Text="✅ One-click optimization profiles (Gaming/Balanced/Eco)"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4" />
                        <TextBlock Text="✅ Advanced system tweaking (services, startup, registry)"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4" />
                        <TextBlock Text="✅ System protection (restore points, backups, rollback)"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4" />
                        <TextBlock Text="✅ Historical analytics with beautiful charts"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4" />
                        <TextBlock Text="✅ Three monitoring modes (Active/Background/Paused)"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,4" />
                        <TextBlock Text="✅ Administrator privilege handling with clear indicators"
                                  Style="{DynamicResource SecondaryTextBlockStyle}" Margin="0,4,0,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <Grid Grid.Row="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Content="Check for Updates" Style="{DynamicResource PrimaryButtonStyle}"
                       Padding="16,12" Margin="0,0,0,12" />

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="View Changelog" Style="{DynamicResource SecondaryButtonStyle}"
                           Padding="16,12" Margin="0,0,12,0" />
                    <Button Grid.Column="1" Content="Open Source Licenses" Style="{DynamicResource SecondaryButtonStyle}"
                           Padding="16,12" Margin="0,0,12,0" />
                    <Button Grid.Column="2" Content="Report Issue" Style="{DynamicResource SecondaryButtonStyle}"
                           Padding="16,12" />
                </Grid>

                <TextBlock Grid.Row="2" Text="Powered by Event Horizon Design System"
                          Style="{DynamicResource SecondaryTextBlockStyle}" TextAlignment="Center"
                          Margin="0,12,0,0" />
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
