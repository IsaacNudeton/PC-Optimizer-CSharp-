<UserControl x:Class="PCOptimizer.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource DeepBlackBrush}"
             Foreground="{DynamicResource PrimaryTextBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{DynamicResource DeepBlackBrush}">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- System Performance Overview - Hero Section -->
            <Border Grid.Row="0" Style="{DynamicResource CardStyle}" Margin="0,0,0,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <TextBlock Grid.Row="0" Text="System Performance Overview"
                              Style="{DynamicResource H3TextBlockStyle}" Margin="0,0,0,16" />

                    <!-- Gauges Row -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- CPU Usage Gauge -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center" Margin="0,0,16,0">
                            <Ellipse Width="120" Height="120" Fill="{DynamicResource LighterCharcoalBrush}"
                                    Stroke="{DynamicResource GoldBrightBrush}" StrokeThickness="2" Margin="0,0,0,12" />
                            <TextBlock Text="CPU" Style="{DynamicResource H3TextBlockStyle}"
                                      TextAlignment="Center" Margin="0,0,0,4" />
                            <TextBlock x:Name="CpuValueText" Text="0%" Style="{DynamicResource MetricsTextBlockStyle}"
                                      TextAlignment="Center" />
                        </StackPanel>

                        <!-- GPU Usage Gauge -->
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center" Margin="0,0,16,0">
                            <Ellipse Width="120" Height="120" Fill="{DynamicResource LighterCharcoalBrush}"
                                    Stroke="{DynamicResource GoldMediumBrush}" StrokeThickness="2" Margin="0,0,0,12" />
                            <TextBlock Text="GPU" Style="{DynamicResource H3TextBlockStyle}"
                                      TextAlignment="Center" Margin="0,0,0,4" />
                            <TextBlock x:Name="GpuValueText" Text="0%" Style="{DynamicResource MetricsTextBlockStyle}"
                                      TextAlignment="Center" />
                        </StackPanel>

                        <!-- RAM Usage Gauge -->
                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <Ellipse Width="120" Height="120" Fill="{DynamicResource LighterCharcoalBrush}"
                                    Stroke="{DynamicResource CopperBrush}" StrokeThickness="2" Margin="0,0,0,12" />
                            <TextBlock Text="RAM" Style="{DynamicResource H3TextBlockStyle}"
                                      TextAlignment="Center" Margin="0,0,0,4" />
                            <TextBlock x:Name="RamValueText" Text="0%" Style="{DynamicResource MetricsTextBlockStyle}"
                                      TextAlignment="Center" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <!-- Quick Actions Grid -->
            <Border Grid.Row="1" Style="{DynamicResource CardStyle}" Margin="0,0,0,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <TextBlock Grid.Row="0" Text="Quick Actions" Style="{DynamicResource H3TextBlockStyle}"
                              Margin="0,0,0,16" />

                    <!-- Button Grid -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="80" />
                            <RowDefinition Height="80" />
                        </Grid.RowDefinitions>

                        <!-- Gaming Mode -->
                        <Button Grid.Column="0" Grid.Row="0"
                               Command="{Binding GamingModeCommand}"
                               Background="{DynamicResource DarkGrayBrush}"
                               BorderBrush="{DynamicResource GoldBrightBrush}"
                               BorderThickness="2"
                               Margin="0,0,12,12"
                               Padding="12"
                               Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="12"
                                           Padding="{TemplateBinding Padding}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="🎮" FontSize="24" TextAlignment="Center" Margin="0,0,0,4" />
                                            <TextBlock Text="Gaming Mode" Style="{DynamicResource SecondaryTextBlockStyle}"
                                                      TextAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Balanced Mode -->
                        <Button Grid.Column="1" Grid.Row="0"
                               Command="{Binding BalancedModeCommand}"
                               Background="{DynamicResource DarkGrayBrush}"
                               BorderBrush="{DynamicResource GoldBrightBrush}"
                               BorderThickness="2"
                               Margin="0,0,12,12"
                               Padding="12"
                               Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="12"
                                           Padding="{TemplateBinding Padding}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="⚡" FontSize="24" TextAlignment="Center" Margin="0,0,0,4" />
                                            <TextBlock Text="Balanced" Style="{DynamicResource SecondaryTextBlockStyle}"
                                                      TextAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Eco Mode -->
                        <Button Grid.Column="2" Grid.Row="0"
                               Command="{Binding EcoModeCommand}"
                               Background="{DynamicResource DarkGrayBrush}"
                               BorderBrush="{DynamicResource GoldBrightBrush}"
                               BorderThickness="2"
                               Margin="0,0,0,12"
                               Padding="12"
                               Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="12"
                                           Padding="{TemplateBinding Padding}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="🌿" FontSize="24" TextAlignment="Center" Margin="0,0,0,4" />
                                            <TextBlock Text="Eco Mode" Style="{DynamicResource SecondaryTextBlockStyle}"
                                                      TextAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Disk Cleanup -->
                        <Button Grid.Column="0" Grid.Row="1"
                               Command="{Binding DiskCleanupCommand}"
                               Background="{DynamicResource DarkGrayBrush}"
                               BorderBrush="{DynamicResource GoldBrightBrush}"
                               BorderThickness="2"
                               Margin="0,0,12,0"
                               Padding="12"
                               Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="12"
                                           Padding="{TemplateBinding Padding}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="🧹" FontSize="24" TextAlignment="Center" Margin="0,0,0,4" />
                                            <TextBlock Text="Disk Cleanup" Style="{DynamicResource SecondaryTextBlockStyle}"
                                                      TextAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Advanced Tweaks -->
                        <Button Grid.Column="1" Grid.Row="1"
                               Command="{Binding AdvancedTweaksCommand}"
                               Background="{DynamicResource DarkGrayBrush}"
                               BorderBrush="{DynamicResource GoldBrightBrush}"
                               BorderThickness="2"
                               Margin="0,0,12,0"
                               Padding="12"
                               Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="12"
                                           Padding="{TemplateBinding Padding}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="🔧" FontSize="24" TextAlignment="Center" Margin="0,0,0,4" />
                                            <TextBlock Text="Advanced Tweaks" Style="{DynamicResource SecondaryTextBlockStyle}"
                                                      TextAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- System Backup -->
                        <Button Grid.Column="2" Grid.Row="1"
                               Command="{Binding SystemBackupCommand}"
                               Background="{DynamicResource DarkGrayBrush}"
                               BorderBrush="{DynamicResource GoldBrightBrush}"
                               BorderThickness="2"
                               Margin="0,0,0,0"
                               Padding="12"
                               Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="12"
                                           Padding="{TemplateBinding Padding}">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="🛡️" FontSize="24" TextAlignment="Center" Margin="0,0,0,4" />
                                            <TextBlock Text="System Backup" Style="{DynamicResource SecondaryTextBlockStyle}"
                                                      TextAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Grid>
            </Border>

            <!-- Status Footer -->
            <Border Grid.Row="2" Style="{DynamicResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Status" Style="{DynamicResource H3TextBlockStyle}" Margin="0,0,0,8" />
                    <TextBlock x:Name="StatusText" Text="Monitoring Active"
                              Style="{DynamicResource BodyTextBlockStyle}" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
